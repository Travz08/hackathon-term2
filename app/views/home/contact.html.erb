<div class="row align-items-center">
<div class="col-md-10 mx-auto">
<h2 class="center">CONTACT</h2></br>
  <div class="text-black center">
  <p>At Pawesome, we are always happy to hear from you. Please use the form below to contact us.</p>

  <%= form_with(model: @contact, method: :post) do |form|%>
  <div class="form-group">
  <%= form.text_field :name, placeholder: 'Name' %>
  </div>
  <div class="form-group">
      <p class="form-control-static"><%= current_user.email %></p>
  </div>
  <div class="form-group">
  <%= form.text_area :message, placeholder: 'Enter message', class: 'forms' %>
  </div>
  <%= form.submit 'Submit', class: 'btn' %>
  <% end %>
  <br><p>Alternatively, we can be found at 7 Kelly Street, Ultimo.</p></br>
  </div>
  <div>
    <iframe src = "https://maps.google.com/maps?q=<%= @location.latitude %>,<%= @location.longitude %>&hl=es;z=14&amp;output=embed" class = "map" ></iframe>
  </div>
</div>
</div>
